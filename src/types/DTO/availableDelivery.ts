
export type IAvailableDelivery = {
    city: string
    type: string
    available: boolean
    price: number
}

export type IAvailableDeliveryID = {
    city: string
    type: string
    available: boolean
    price: number
    id: number
}

// city: "nur-sultan", type: "pickup", available: false, "price": 0.00
